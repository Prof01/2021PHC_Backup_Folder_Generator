// import { axios } from "axios";

const mainForm = document.querySelector("#mainForm")
const totalSAs = document.querySelector("input[name='totalSAs']")
const distCode = document.querySelector("input[name='distCode']")
const distName = document.querySelector("input[name='distName']")

// mainForm.addEventListener('submit', e => {
//     e.preventDefault();
//     const payload = {
//         totalSAs: totalSAs.value,
//         distCode: distCode.value,
//         distName: distName.value
//     }

//     console.log(payload);
// })